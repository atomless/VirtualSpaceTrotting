# TODO

## Next

- Complete live Linode deployment once `LINODE_TOKEN` is available. Proof: `make deploy-linode-one-shot ...` succeeds and hosted `/health` returns 200.
- Consider replacing procedural preview imagery with a richer AI-generated satellite-style batch. Proof: provenance records model/prompt metadata and every generated asset passes the site content contract.
